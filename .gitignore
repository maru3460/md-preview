# ビルド成果物
target/

# ローカル設定
.claude/
CLAUDE.md

# macOS
.DS_Store

# playwright-cli の作業ディレクトリ（動作確認の一時成果物）
.playwright-cli/

# UI スモークテスト（Playwright）の依存と成果物。package.json は追跡する。
node_modules/
package-lock.json
test-results/
playwright-report/

# UI テストが一時的に置くフィクスチャ（差分・巨大ソース用。テスト内で消すが、落ちた時の保険）
tests/ui-fixtures/zz-untracked.md
tests/ui-fixtures/zz-big.txt
